alias True true .
alias ⊤ true .
alias constantly true .

alias ⊥ false .
alias False false .

def not fn p
  p false true .

alias ¬ not .
alias ⫟ not .
alias ~ not .
alias false? not .

def and fn (p q)
  p q p .

alias ∧ and .
alias & and .

def or fn (p q)
  p p q .

alias | or .
alias ∨ or .

def xor fn (p q)
  p (not q) q .

alias ⊻ xor .
alias ⊕ xor .
aliias ⩛ xor .

def nand fn (p q)
  not : and p q .

alias ⊼ nand .
alias ↑ nand .

def nor fn (p q)
  not : or p q .

alias ⊽ nor .
alias ↓ nor .

def xnor fn (p q)
  not : xor p q .

alias ⊙ xnor .
alias <-> xnor .
alias ↔ xnor .
alias ⇔ xnor .
alias ⬄ xnor .
alias ⟺ xnor .
alias bool= xnor .

def impl fn (p q)
  or (not p) q .

alias ⇒ impl .
alias Ɔ impl .
alias ⟹ impl .
alias ⊃ impl .

def mux fn (p q s)
  or (and p (not s)) (and q s) .

def complement fn (p b)
  not : p b .

def conjoin fn (a b x)
  and (a x) (b x) .

def disjoin fn (a b x)
  or (a x) (b x) .

def always fn x true .
def never  fn x false .
